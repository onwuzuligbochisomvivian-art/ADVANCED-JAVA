window.NOTES = {"1": {"title": "OOP Fundamentals", "summary": "Object-oriented programming organizes software around objects that combine state and behavior. The four core ideas are encapsulation, inheritance, polymorphism and abstraction.", "topics": [["What is OOP?", "OOP is a programming approach built around classes and objects. A class is a blueprint; an object is an instance created from that blueprint. OOP improves organization, reuse, maintainability and extensibility."], ["Encapsulation", "Bundle fields and methods in a class and control direct access with modifiers. A common pattern is private fields plus public getters/setters or other controlled methods."], ["Inheritance", "A subclass inherits accessible members from a superclass using extends. Java supports single direct class inheritance. Use inheritance for a genuine is-a relationship."], ["Polymorphism", "A parent reference can refer to a child object. With overriding, the method selected for an instance call is determined at runtime (dynamic dispatch)."], ["Abstraction", "Expose essential behavior while hiding unnecessary implementation details. Abstract classes and interfaces are common Java mechanisms."], ["Interfaces", "An interface defines a contract. A class uses implements and may implement multiple interfaces. Interfaces can contain abstract methods and, in modern Java, default/static methods."]], "code": "class Animal {\n    void sound() { System.out.println(\"Animal\"); }\n}\nclass Dog extends Animal {\n    @Override\n    void sound() { System.out.println(\"Dog\"); }\n}\nAnimal a = new Dog();\na.sound();   // Dog", "exam": "Know the difference between overloading (same method name, different parameter list; compile-time selection) and overriding (subclass replaces inherited method; runtime dispatch)."}, "2": {"title": "Class Hierarchies, Packages & Java APIs", "summary": "Java programs are organized into classes and packages. Class hierarchies express inheritance relationships, while standard APIs provide reusable classes and methods.", "topics": [["Program organization", "A Java source file contains declarations such as classes, interfaces and imports. A class contains fields (state) and methods (behavior)."], ["Packages", "Packages group related types, reduce naming conflicts and help control visibility. import lets a source file refer to types without repeatedly writing their fully qualified names."], ["Access modifiers", "public: broadly accessible; protected: same package and subclasses (with Java's access rules); default/package-private: same package; private: declaring class only."], ["Class hierarchy", "Object is the root of Java's class hierarchy. A subclass uses extends. Java permits one direct superclass for a class."], ["Abstract classes", "An abstract class cannot be instantiated directly. It may have fields, constructors, concrete methods and abstract methods."], ["Interfaces", "Interfaces describe contracts. A class implements an interface and can implement multiple interfaces."], ["Object methods", "Every class ultimately inherits Object methods such as toString(), equals(Object) and hashCode()."], ["Java Standard APIs", "The Java library provides prewritten classes, interfaces and methods. Common packages include java.lang, java.util and java.io."]], "code": "abstract class Animal {\n    protected String name;\n    abstract void makeSound();\n    void breathe() { System.out.println(\"breathing\"); }\n}\nclass Dog extends Animal {\n    @Override\n    void makeSound() { System.out.println(\"woof\"); }\n}\nAnimal a = new Dog();\na.makeSound();", "exam": "Remember: a class can extend only one direct class, but it can implement multiple interfaces. Do not confuse an abstract class with an interface."}, "3": {"title": "Collections: Iterators, Lists, Stacks & Queues", "summary": "The Collections Framework provides reusable data structures and traversal tools. Learn the interfaces, the behavior of each collection and the important method pairs.", "topics": [["Iterator", "Iterator traverses a collection using hasNext() and next(). remove() removes the last element returned by next(), when supported."], ["Enumeration", "Enumeration is a legacy traversal mechanism using hasMoreElements() and nextElement(). It does not provide Iterator's remove()."], ["List", "A List is ordered, index-based and permits duplicates. ArrayList is backed by a dynamic array; LinkedList uses linked nodes."], ["ListIterator", "ListIterator works with Lists and can move forward and backward. It supports hasNext(), next(), hasPrevious(), previous(), remove(), set() and add()."], ["Stack", "Stack is LIFO. push adds to the top; pop removes the top; peek reads the top without removing it. Empty pop/peek throws EmptyStackException."], ["Queue", "Queue is normally FIFO. offer/add inserts; poll/remove removes; peek/element inspects the head. poll/peek return null when empty, while remove/element throw."], ["PriorityQueue", "PriorityQueue orders elements by priority rather than ordinary insertion order. With natural ordering, the smallest integer has highest priority."], ["HashSet / HashMap / TreeSet", "HashSet stores unique elements; HashMap stores key-value mappings; TreeSet keeps unique elements sorted according to its ordering."], ["Fail-fast vs fail-safe", "Fail-fast iterators detect structural modification and commonly throw ConcurrentModificationException. Fail-safe terminology is commonly used for traversal over a snapshot/copy or weakly consistent view rather than directly failing on the same modification."]], "code": "List<Integer> list = new ArrayList<>();\nlist.add(10);\nlist.add(20);\n\nIterator<Integer> it = list.iterator();\nwhile (it.hasNext()) {\n    System.out.println(it.next());\n}\n\nQueue<Integer> q = new LinkedList<>();\nq.offer(10);\nq.offer(20);\nSystem.out.println(q.poll()); // 10", "exam": "Memorize method behavior, especially poll vs remove and peek vs element, and Stack's LIFO versus Queue's FIFO."}, "4": {"title": "Iterator Pattern & Fail-Fast/Fail-Safe", "summary": "The Iterator design pattern separates traversal from the collection being traversed. Java's iterator behavior also creates common exam questions about structural modification.", "topics": [["Design patterns", "A design pattern is a reusable design idea for a recurring software problem. It is not a ready-made program."], ["Iterator Pattern", "Provides a standard way to access elements sequentially without exposing the collection's internal representation."], ["Custom iterators", "A custom iterator normally maintains traversal state and implements methods such as hasNext() and next()."], ["Fail-fast", "A fail-fast iterator detects certain structural modifications made outside the iterator and commonly throws ConcurrentModificationException."], ["Fail-safe terminology", "The term is often used for traversal that works on a separate copy/snapshot or uses weakly consistent behavior. Exact behavior depends on the collection implementation."], ["Safe modification", "When supported, use Iterator.remove() rather than modifying the collection directly during iteration."]], "code": "List<String> names = new ArrayList<>();\nnames.add(\"A\");\nnames.add(\"B\");\n\nIterator<String> it = names.iterator();\nwhile (it.hasNext()) {\n    String x = it.next();\n    if (x.equals(\"A\")) {\n        it.remove();\n    }\n}", "exam": "The iterator's position is between elements. hasNext() checks whether another element exists; next() advances and returns it."}, "5": {"title": "Searching Algorithms", "summary": "Searching means locating a target value. Linear search works without sorting; binary search repeatedly halves a sorted search space.", "topics": [["Linear search", "Check elements one by one from the beginning until the target is found or the collection ends. It does not require sorted data."], ["Binary search", "Requires sorted data. Compare the target with the middle element, then discard half of the remaining range."], ["Binary-search boundaries", "If target < middle, move high to mid-1. If target > middle, move low to mid+1."], ["Arrays.binarySearch", "Java's Arrays.binarySearch can search a sorted array and returns an index when the target is found."], ["Early exit", "A loop can use break to stop immediately after finding a target."], ["Complexity", "Linear search is O(n) in the worst case. Binary search is O(log n) because the search interval is repeatedly halved."]], "code": "int[] a = {2, 5, 8, 12, 17};\nint target = 12;\nint low = 0, high = a.length - 1;\n\nwhile (low <= high) {\n    int mid = (low + high) / 2;\n    if (a[mid] == target) {\n        System.out.println(mid);\n        break;\n    } else if (target < a[mid]) {\n        high = mid - 1;\n    } else {\n        low = mid + 1;\n    }\n}", "exam": "Binary search is not simply 'faster search'; its key condition is that the data/search range is ordered."}, "6": {"title": "Sorting Algorithms & Complexity", "summary": "Sorting arranges data according to an order. Know how each algorithm identifies or moves elements, its complexity, and its strengths and weaknesses.", "topics": [["Bubble sort", "Repeatedly compares adjacent elements and swaps them when out of order. An optimized version can stop early when no swap occurs."], ["Selection sort", "Find the minimum (or maximum) in the unsorted part and swap it into its final position."], ["Insertion sort", "Treat the left side as sorted, store the current value as key, shift larger values and insert key."], ["Merge sort", "Divide the array into halves, sort each half, then merge the sorted halves. Time is O(n log n); standard implementations use O(n) extra space."], ["Quick sort", "Choose a pivot, partition elements around it, then recursively sort the partitions. Average time is O(n log n); worst case can be O(n²)."], ["Heap sort", "Build a heap and repeatedly move the root to the sorted region. Standard in-place heap sort is O(n log n) time and O(1) auxiliary space."], ["Stability", "Stable sorting preserves the relative order of equal-key records. Bubble, insertion and merge are commonly stable; standard selection, quick and heap sorts are not inherently stable."], ["Big-O", "Big-O describes asymptotic growth. Typical ordering from smaller to larger growth is O(1), O(log n), O(n), O(n log n), O(n²), O(2^n)."], ["Other notations", "Ω describes an asymptotic lower bound; Θ gives a tight asymptotic bound."], ["Time vs space", "Time complexity describes operation growth; space complexity describes additional memory growth."]], "code": "// Insertion sort\nfor (int i = 1; i < a.length; i++) {\n    int key = a[i];\n    int j = i - 1;\n\n    while (j >= 0 && a[j] > key) {\n        a[j + 1] = a[j];\n        j--;\n    }\n    a[j + 1] = key;\n}", "exam": "Code-recognition clues: Bubble → adjacent pair; Selection → min/max; Insertion → key + shifting; Merge → mid + merge; Quick → pivot + partition; Heap → heapify + child indices."}, "7": {"title": "Recursive Algorithms", "summary": "Recursion solves a problem by having a method call itself on a smaller instance. Every correct recursive solution needs a base case and progress toward it.", "topics": [["Fundamentals", "A recursive method calls itself. The base case stops recursion; the recursive case reduces the problem."], ["GCD", "Euclid's algorithm uses gcd(a,b)=gcd(b,a%b) until the second value reaches zero."], ["Reverse a string", "A recursive solution can process the first/last character and solve the smaller substring."], ["QuickSort", "QuickSort is naturally recursive: partition around a pivot, then recursively sort the partitions."], ["Tower of Hanoi", "Move n-1 disks to the auxiliary peg, move the largest disk, then move n-1 disks to the destination."], ["Exponentiation", "Recursive exponentiation can reduce the exponent; fast exponentiation can reduce work using squaring."], ["Permutations", "Generate arrangements by choosing an element, recursively arranging the remainder, then restoring/swapping as needed."], ["Stack memory", "Each recursive call consumes stack space. Excessive depth can cause StackOverflowError."], ["Call overhead", "Recursion adds function-call overhead and may use more memory than an iterative equivalent."], ["Memoization", "Store results of repeated recursive subproblems to avoid recomputation."], ["Recursion vs iteration", "Iteration often uses less call-stack memory; recursion can express naturally recursive structures and divide-and-conquer algorithms clearly."]], "code": "static int gcd(int a, int b) {\n    if (b == 0) {\n        return a;          // base case\n    }\n    return gcd(b, a % b);  // smaller problem\n}", "exam": "If there is no reachable base case, recursion does not terminate normally. In Java, deep recursion can produce StackOverflowError."}, "8": {"title": "Event-Driven Programming & Event Handling", "summary": "Event-driven programs respond to actions or events such as button clicks, key presses and mouse actions. The program reacts through event sources, events and listeners/handlers.", "topics": [["Event-driven programming", "Program flow is influenced by events rather than only a fixed top-to-bottom sequence."], ["Event source", "The component that generates an event, such as a button."], ["Event object", "Carries information about what happened."], ["Listener/handler", "Code registered to respond to a particular event type."], ["Registration", "A listener is attached to an event source so the source can notify it when the event occurs."], ["Common GUI events", "Action events, mouse events and keyboard events are common examples."], ["Event propagation", "An event may be dispatched through a framework's event mechanism to the appropriate registered handler."]], "code": "JButton button = new JButton(\"Click\");\nbutton.addActionListener(e -> {\n    System.out.println(\"Button clicked\");\n});", "exam": "Know the relationship: source produces event → framework dispatches it → registered listener/handler responds."}, "9": {"title": "Exception Handling", "summary": "Exceptions represent abnormal conditions that disrupt normal program flow. Java provides try, catch, finally, throw and throws to handle or declare exceptional situations.", "topics": [["Exception basics", "An exception is an object representing an abnormal condition. Unhandled exceptions can terminate the current flow."], ["try/catch", "Put risky code in try and provide matching catch blocks for exceptions you can handle."], ["finally", "A finally block is generally used for cleanup that should occur whether an exception is thrown or not."], ["throw", "Explicitly creates/throws an exception object."], ["throws", "Declares that a method may pass specified checked exceptions to its caller."], ["Checked vs unchecked", "Checked exceptions are checked by the compiler and generally must be caught or declared. RuntimeException subclasses are unchecked."], ["Exception hierarchy", "Throwable is the top-level superclass for errors and exceptions. Exception and Error are major branches."], ["Multiple catch", "Catch blocks should be ordered from more specific to more general types."]], "code": "try {\n    int x = 10 / 0;\n} catch (ArithmeticException e) {\n    System.out.println(\"Cannot divide by zero\");\n} finally {\n    System.out.println(\"Done\");\n}", "exam": "Do not confuse throw (actually throws one exception) with throws (declares possible exceptions in a method signature)."}, "10": {"title": "GUI Programming", "summary": "Java GUI programming builds interfaces from components, containers, layouts and event handlers. Swing is a common Java GUI toolkit taught in introductory courses.", "topics": [["Components and containers", "A component is a UI element such as JButton or JLabel. A container holds components, such as JFrame or JPanel."], ["Common components", "JLabel displays text, JTextField accepts text, JButton triggers actions, JCheckBox represents an independent choice, JRadioButton is commonly grouped for one-of-many selection."], ["Layouts", "Layout managers control component arrangement. Common ones include FlowLayout, BorderLayout and GridLayout."], ["Frames and panels", "A JFrame can serve as a top-level window; JPanel is commonly used to group components."], ["Event handling", "GUI components generate events and listeners respond to them."], ["GUI lifecycle", "Create components, arrange them, attach listeners, configure the window and make it visible."]], "code": "JFrame frame = new JFrame(\"Demo\");\nJButton button = new JButton(\"Click\");\nframe.add(button);\nframe.setSize(300, 200);\nframe.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);\nframe.setVisible(true);", "exam": "Separate the roles: component = UI element; container = holds components; layout manager = controls arrangement; listener = responds to events."}, "11": {"title": "Strings & String Processing", "summary": "Strings are objects representing sequences of characters. Java String is immutable, so operations that appear to change a String usually produce another String.", "topics": [["String creation", "String literals can be written with quotes; String objects can also be created explicitly."], ["Immutability", "Once created, a String object's character sequence cannot be changed. Methods such as concat or replace return a new String."], ["Comparison", "Use equals() for content equality. == compares references when used with objects."], ["Common methods", "length(), charAt(), substring(), indexOf(), contains(), startsWith(), endsWith(), toUpperCase(), toLowerCase(), trim() and split() are common."], ["StringBuilder", "StringBuilder is mutable and is useful for repeated modifications such as appending in loops."], ["Parsing", "Methods such as Integer.parseInt() convert suitable numeric text to primitive values."]], "code": "String s = \"Java\";\nSystem.out.println(s.length());      // 4\nSystem.out.println(s.charAt(1));     // a\nSystem.out.println(s.substring(1, 3)); // av", "exam": "substring(begin, end) uses an inclusive beginning index and an exclusive ending index."}, "12": {"title": "Simple I/O, Files & Arrays", "summary": "Java provides classes for console input/output, file handling and array processing. Arrays have fixed length and zero-based indexes.", "topics": [["Console output", "System.out.print(), println() and printf() write output."], ["Scanner input", "Scanner can read primitive values and text from sources such as System.in."], ["Arrays", "An array stores a fixed number of values of one declared type. Indexing starts at 0 and the last index is length-1."], ["Array traversal", "Use ordinary for loops when an index is needed, or enhanced for loops when you only need each element."], ["Files and streams", "Java I/O classes provide mechanisms for reading and writing data. Resource management is important when working with streams."], ["Arrays utility class", "java.util.Arrays provides methods such as sort(), binarySearch(), fill(), equals() and toString()."]], "code": "int[] nums = {4, 7, 2, 9};\n\nfor (int i = 0; i < nums.length; i++) {\n    System.out.println(nums[i]);\n}\n\nArrays.sort(nums);\nint pos = Arrays.binarySearch(nums, 7);", "exam": "Remember arrays use length (a field), while Strings use length() (a method)."}, "13": {"title": "Control Structures, Methods & Integrated Java", "summary": "Control structures determine program flow. Methods organize reusable behavior. Integrated exam questions often combine variables, operators, conditions, loops, arrays, methods and OOP.", "topics": [["Selection", "if, if-else and switch choose among alternatives."], ["Loops", "for is useful when the iteration structure is known; while checks before each iteration; do-while executes its body at least once."], ["break and continue", "break exits the loop/switch; continue skips the remainder of the current loop iteration."], ["Methods", "A method has a return type, name, parameter list and body. Parameters receive arguments when the method is called."], ["Pass-by-value", "Java passes arguments by value. For object references, the reference value is copied; a method can mutate the referenced object but cannot replace the caller's reference variable."], ["Method overloading", "Same method name with different parameter lists."], ["Integrated tracing", "Trace initialization, condition checks, updates, method calls and return values in order. Watch integer division and operator precedence."]], "code": "static int add(int a, int b) {\n    return a + b;\n}\n\nfor (int i = 0; i < 3; i++) {\n    if (i == 1) continue;\n    System.out.println(add(i, 2));\n}", "exam": "When tracing code, write variable values after each important statement. Do not mentally skip loop updates or return statements."}};